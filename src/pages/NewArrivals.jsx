import CollectionPage from './CollectionPage'

/**
 * New Arrivals is intentionally lightweight: it lists the pieces the store is
 * currently featuring. Fresh stock changes frequently and is shared live on
 * Instagram and WhatsApp rather than invented here.
 */
export const NewArrivals = () => <CollectionPage slug="new-arrivals" />
export default NewArrivals